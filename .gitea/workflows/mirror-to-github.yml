name: Mirror to GitHub

on:
  push:
    branches:
      - main

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Push to GitHub
        run: |
          git push --mirror https://${{ secrets.GITHUB_PAT }}@github.com/kay4pres/trading-agent.git

name: Minimal Test

on:
  push:
    branches:
      - dev

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Test
        run: |
          echo "Runner is working!"
          whoami
          pwd
          ls -la
          echo "Done"

# Trading Agent — Docker image dependencies
# Install: pip install -r requirements.txt

# Web framework
Flask>=3.0.0
flask-cors>=4.0.0

# Data providers
yfinance>=0.2.36
requests>=2.31.0
tradingview-screener>=0.5.0

# Telegram bot
python-telegram-bot>=20.7

# Process management inside container
croniter>=2.0.0

# Data handling
pandas>=2.0.0
numpy>=1.26.0

# Alpaca trading execution
# (was >=7.0.0 — typo, no such version. actual current is 0.43.5 per UAT container
# and PyPI. floor at 0.40 for headroom.)
alpaca-py>=0.40.0

# Testing (Dev environment only — required for smoke_e2e.py)
pytest>=7.0.0

# Local model (Whisper)
# faster-whisper is installed separately — see Dockerfile
